import type { NextConfig } from "next";

const nextConfig: any = {
  /* config options here */
  allowedDevOrigins: ['svv-arno-test.loca.lt', '192.168.1.164'],
  devIndicators: false,
};

export default nextConfig as NextConfig;