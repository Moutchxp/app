import { EnTetePage } from '../_composants/EnTetePage';

export default function InternautesPage() {
  return (
    <section style={{ maxWidth: 720 }}>
      <EnTetePage titre="Internautes" intro="Module à venir." />
    </section>
  );
}
