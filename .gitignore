# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# données géographiques volumineuses (dalles LiDAR, etc.) — JAMAIS committées
/app/data/

# BDNB open data (année de construction, scratch dept 92) — JAMAIS committée
/data/bdnb/

# Parcs & jardins OCS GE (scratch d'inspection) — JAMAIS committée
/data/parcs_jardins/

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# certificats HTTPS de dev (clé privée — jamais committée)
/certs
certificates/

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
